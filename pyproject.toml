[tool.uv]
package = false

[project]
name = "ecochainai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Suman", email = "sumankuyya@gmail.com" }
]
requires-python = ">=3.9"
dependencies = []

[project.scripts]
ecochainai = "ecochainai:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
